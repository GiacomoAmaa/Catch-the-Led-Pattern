#include "Buttons.h"
#include <Arduino.h>
/*
int get_interrupt_pin(int* button_pin, int* index){
  for(int i = 0; i < 4 ; i++){
    if(digitalRead(button_pin[i]) == HIGH){
      *index = i;
      return button_pin[i];
    }
  }
  return -1;
}*/